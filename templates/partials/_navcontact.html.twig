<nav class="navbar navbar-expand-lg bg-body-tertiary NavContact">
  <div class="container-fluid">
    <!-- Titre -->
    <span class="navbar-brand fw-bold">Contactez-nous</span>

    <!-- Bouton mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavContact" aria-controls="navbarNavContact" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Contenu -->
    <div class="collapse navbar-collapse" id="navbarNavContact">
      <div class="d-flex flex-column flex-lg-row justify-content-between align-items-center w-100 px-4 py-3">
        
        <!-- Partie gauche (bloc contact avec grands espacements) -->
        <div class="d-flex flex-column flex-lg-row justify-content-center align-items-center text-center gap-5 mx-auto">
          
          <div class="nav-item mx-3">
            <p class="mb-0">
              <strong>Par téléphone :</strong><br>
              07 00 00 00 00<br>
              01 00 00 00 00
            </p>
          </div>

          <div class="nav-item mx-3">
            <p class="mb-0">
              <strong>Par email :</strong><br>
              capnumerique@gmail.com
            </p>
          </div>
        </div>

        <!-- Partie droite (logos + paragraphe) -->
        <div class="d-flex flex-column align-items-end gap-2">

        <!-- Paragraphe au-dessus -->
        <p class="mb-1 text-end">
            Suivez-nous sur nos réseaux sociaux
        </p>

        <!-- Logos -->
        <div class="d-flex justify-content-end align-items-center gap-4">
            <a class="nav-link p-0" href="#" aria-label="Facebook">
            <img src="{{ asset('uploads/img_home/LogoFacebook.png') }}" alt="Logo Facebook" width="35">
            </a>
            <a class="nav-link p-0" href="#" aria-label="Instagram">
            <img src="{{ asset('uploads/img_home/InstagramLogo.png') }}" alt="Logo Instagram" width="35">
            </a>
            <a class="nav-link p-0" href="#" aria-label="TikTok">
            <img src="{{ asset('uploads/img_home/LogoTikTok.png') }}" alt="Logo TikTok" width="35">
            </a>
            <a class="nav-link p-0" href="#" aria-label="Twitter/X">
            <img src="{{ asset('uploads/img_home/LogoTwitterX.png') }}" alt="Logo Twitter/X" width="35">
            </a>
            <a class="nav-link p-0" href="#" aria-label="Youtube">
            <img src="{{ asset('uploads/img_home/Youtube_logo.png') }}" alt="Logo Youtube" width="35">
            </a>
        </div>

        </div>
      </div>
    </div>
  </div>
</nav>
